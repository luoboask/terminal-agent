# 🧪 边界场景测试计划

**测试时间**: 2026-04-06 18:52  
**目标**: 验证优化后的鲁棒性

---

## 📋 测试场景

### 场景 1: 真正需要重复的任务
```bash
"创建 3 个文件：a.txt, b.txt, c.txt"
# 预期：应该允许 3 次 file_write（参数不同）
```

### 场景 2: 相似但不同的命令
```bash
"先 mkdir test-dir，然后在里面创建文件"
# 预期：mkdir 和 file_write 是不同工具，应该允许
```

### 场景 3: 错误后重试
```bash
"创建一个不合法路径的文件 /root/test.txt"
# 预期：失败后不应无限重试
```

### 场景 4: 多步骤依赖任务
```bash
"创建目录 my-project，在里面创建 package.json，然后用 cat 查看"
# 预期：顺序执行，每步完成后继续
```

### 场景 5: 模糊请求
```bash
"帮我做点什么"
# 预期：询问用户具体需求，而不是盲目调用工具
```

---

开始测试...
